spring.application.name=HospitalSystem

# Enable the H2 Console
spring.h2.console.enabled=true

# This sets the path (e.g., localhost:8080/h2-console)
spring.h2.console.path=/h2-console

# Fixes the "Refused to display in a frame" error
spring.headers.frame-options=sameorigin

# Keep the database name consistent so you know what to type in the login
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password